version: "2"

linters:
  default: none
  enable:
    - govet
    - errcheck
    - staticcheck
    - ineffassign
    - gofmt
    - misspell

issues:
  exclude-rules:
    # Test helpers commonly ignore errors intentionally.
    - path: _test\.go
      linters:
        - errcheck
