run:
  timeout: 5m

linters:
  disable-all: true
  enable:
    - govet
    - errcheck
    - staticcheck
    - ineffassign
    - gofmt
    - misspell

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
